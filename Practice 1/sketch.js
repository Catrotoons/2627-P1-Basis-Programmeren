function setup() {
  createCanvas(800, 600); //width and length of canvas. Add ; at the end of every code line.
  
}

function draw() {
  background("teal"); // 0-255 greyscale or color name, do everything in between {}. Code reads from top to bottom.

  rect(400, 300, 100,);

  line(800, 0, 0, 200); // x, y, breedte, hooogte.
}

